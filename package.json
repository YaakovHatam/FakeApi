{
  "name": "supermarket",
  "version": "1.0.0",
  "description": "1. `npm init --yes`\r 2. `npm install -g json-server`\r 3. Create new file called `db.json`\r 4. copy the content from https://raw.githubusercontent.com/YaakovHatam/FakeApi/main/db.json to the opened file\r 5. run the command `json-server --watch db.json`",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "bootstrap": "^4.5.3"
  }
}
